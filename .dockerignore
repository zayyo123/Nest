# 学习注释：这个文件告诉 Docker 构建镜像时“不要把哪些文件复制进镜像”。
# 目的有两个：一是减小镜像体积，二是避免把本地隐私配置或临时文件打包进去。
**/node_modules
# 前后端构建后的产物会在镜像内重新生成，不需要从本机复制。
**/dist
**/coverage
# .env 可能包含数据库密码、JWT 密钥等敏感信息，绝对不要进入镜像上下文。
**/.env
**/.env.local
**/*.log
# .git 只服务于版本控制，应用运行并不需要它。
.git
.idea
.vscode
nestlearn.db
